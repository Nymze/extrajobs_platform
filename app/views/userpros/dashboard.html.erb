<% current_userpro.job_offers.each do |job_offer| %> 

	<b><%= job_offer.title %></b>
	<br>
	<br>
	
	<%= job_offer.description %>
	<br>
	<br>

	<% if !job_offer.users.empty? %>
		<div>	
			Les extras suivants on postulé :
				<ul>
					<li>
						<% job_offer.users.each do |user| %>
							<%=  user.email %>
						<% end %>
					</li>
				</ul>	
		</div>
	<% else %>
		Aucun extra n'a postulé pour le moment.
	<% end %>
		<br>
		<br>	

<% end %>